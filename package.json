{
  "name": "greencarbon-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Working UI prototype for the Stitch Agri logbook field mapper.",
  "scripts": {
    "build": "node -e \"console.log('No build step required for static Vercel deploy')\"",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.2",
    "@supabase/supabase-js": "^2.103.3"
  }
}
